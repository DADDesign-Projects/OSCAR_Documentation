source "https://rubygems.org"

# Jekyll
gem "jekyll", "~> 4.2"   # ou la version que tu veux

# Pour Just the Docs
gem "just-the-docs"

# Recommandé sur Windows
gem "wdm", "~> 0.2.0", :install_if => Gem.win_platform?
gem "tzinfo-data"
gem "jekyll-remote-theme"   # si tu utilises remote_theme dans _config.yml
gem "base64"   # pour les images encodées en base64